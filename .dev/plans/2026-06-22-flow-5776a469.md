# Plan: Clear GitHub Actions Node.js 20 Deprecation Warnings

## Goal

Bump every action that GitHub flags as running on the Node 20 runtime up to its lowest major version that declares `runs.using: node24`. No job-logic or matrix changes — pure action-version bumps.

## Research Summary

### Actions Named in CI Deprecation Annotations

From run 27926009841 (ci workflow, push to main, 2026-06-22) `##[warning]` annotations:

- `actions/checkout@v4` — flagged in all 6 jobs
- `actions/setup-node@v4` — flagged in all 6 jobs
- `pnpm/action-setup@v4` — flagged in all 6 jobs (NOT composite; its action.yml declares `runs.using: node20`)
- `actions/setup-java@v4` — flagged in integration jobs only
- `googleapis/release-please-action@v4` — flagged in release-please workflow

### Verified node24 Versions

All five actions have a v5 major that declares node24. No action is missing a node24 release.

| Action                           | Current | Target | Verified         |
| -------------------------------- | ------- | ------ | ---------------- |
| actions/checkout                 | @v4     | @v5    | node24 confirmed |
| actions/setup-node               | @v4     | @v5    | node24 confirmed |
| pnpm/action-setup                | @v4     | @v5    | node24 confirmed |
| actions/setup-java               | @v4     | @v5    | node24 confirmed |
| googleapis/release-please-action | @v4     | @v5    | node24 confirmed |

**Note on pnpm/action-setup**: The brief notes it as a composite action that might be exempt. However verification shows pnpm/action-setup@v4 declares `runs.using: node20` (not composite). It IS subject to the deprecation and IS explicitly named in CI warning annotations. Bump it to @v5.

## Files to Touch

Only two files:

- `.github/workflows/ci.yml`
- `.github/workflows/release-please.yml`

## Exact Edit List

### .github/workflows/ci.yml

Use global replace (all occurrences in the file):

- `actions/checkout@v4` -> `actions/checkout@v5` (lines 18, 45, 70, 95, 127, 161)
- `pnpm/action-setup@v4` -> `pnpm/action-setup@v5` (lines 20, 47, 72, 102, 134, 163)
- `actions/setup-node@v4` -> `actions/setup-node@v5` (lines 24, 51, 76, 106, 138, 167)
- `actions/setup-java@v4` -> `actions/setup-java@v5` (lines 97, 129)

Do NOT change any `node-version:` input lines (lines 26, 53, 78, 108, 140, 169 with values 20 or 22).

### .github/workflows/release-please.yml

- Line 19: `googleapis/release-please-action@v4` -> `googleapis/release-please-action@v5`
- Line 28: `actions/checkout@v4` -> `actions/checkout@v5`
- Line 31: `pnpm/action-setup@v4` -> `pnpm/action-setup@v5`
- Line 38: `actions/setup-node@v4` -> `actions/setup-node@v5`

## Approach

1. **Set up the worktree**: In the provisioned worktree (NOT the main worktree, which has unrelated uncommitted edits behind origin/main), run:

   ```
   git fetch origin main
   git rebase origin/main
   ```

2. **Apply the edits** using the Edit tool with `replace_all: true` for each pattern, or equivalent sed global replace on both files.

3. **Verify no collateral changes**: Confirm `node-version:` lines (20 and 22) are untouched after the edits.

4. **Open the PR**: Commit, push, open PR targeting `main`. Branch name is whatever dev provisioned.

## Edge Cases

- **pnpm/action-setup@v4**: Despite the brief suggesting it might be composite/exempt, it declares `runs.using: node20` and is named in every annotation. Bump it.
- **googleapis/release-please-action**: Only appears in release-please.yml, not ci.yml. Bump it there.
- **actions/setup-java**: Only appears in ci.yml integration jobs (lines 97, 129). Not in release-please.yml.

## Documentation

No README, docs/, or doc-comment changes required. This is pure CI infrastructure housekeeping.

## Acceptance Verification

1. CI is green on the PR.
2. The `##[warning]Node.js 20 is deprecated` annotation no longer appears in PR CI run logs.
3. `node-version:` values (20 and 22) remain unchanged in both files.
