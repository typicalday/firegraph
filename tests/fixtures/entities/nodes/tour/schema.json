{
  "type": "object",
  "required": ["name", "difficulty"],
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "difficulty": { "type": "string", "enum": ["easy", "medium", "hard"] },
    "maxRiders": { "type": "integer", "minimum": 1 }
  },
  "additionalProperties": false
}
